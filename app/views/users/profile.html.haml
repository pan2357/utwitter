%h1= params[:name]

.row
    .col-10
        = render 'posts_list'
    .col
        - if @followed
            = button_to "Unfollow", profile_url(params[:name]), class: "btn btn-danger", style:"margin: 5px;", method: :delete
            -# = simple_form_for(@follow, url: profile_url(params[:name]), method: "delete") do |f|
            -#     .form-actions.mt-2
            -#         = f.button :submit, "Unfollow", class: "btn btn-danger"
        - else
            = button_to "Follow", profile_url(params[:name]), class: "btn btn-primary", style:"margin: 5px;"
            -# = simple_form_for(@follow, url: profile_url(params[:name])) do |f|
            -#     .form-actions.mt-2
            -#         = f.button :submit, "Follow", class: "btn btn-primary"
= render 'modal'
